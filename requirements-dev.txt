# Development / CI-only tooling. Not required to deploy the app.
-r requirements.txt

ruff>=0.6.0
pytest>=8.0.0
pytest-cov>=5.0.0
mypy>=1.10.0
openapi-spec-validator>=0.7.1,<0.8

# Mirrors lambdas/push_sender/requirements.txt exactly, so this repo's own
# test/type-check environment can actually import google-auth/requests
# when exercising lambdas/push_sender. The DEPLOYED push_sender Lambda
# never installs from this file -- it gets these two (and their pinned
# transitive dependencies) from a Docker-based CDK bundling step; see
# infrastructure/stacks/notification_stack.py and
# docs/fcm-notification-sender.md.
google-auth==2.35.0
requests==2.32.3
