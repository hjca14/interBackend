"""Reusable L2/L3 constructs shared across stacks.

Empty in this phase: no stack yet provisions concrete resources that would
benefit from a shared construct. Add constructs here once a pattern (e.g. a
tagged DynamoDB table, a least-privilege IoT policy builder) is reused by
more than one stack, instead of speculatively creating abstractions now.
"""
